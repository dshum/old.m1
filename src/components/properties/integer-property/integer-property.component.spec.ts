/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { IntegerPropertyComponent } from './integer-property.component';

describe('Component: IntegerProperty', () => {
  it('should create an instance', () => {
    let component = new IntegerPropertyComponent();
    expect(component).toBeTruthy();
  });
});
