/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { VirtualPropertyComponent } from './virtual-property.component';

describe('Component: VirtualProperty', () => {
  it('should create an instance', () => {
    let component = new VirtualPropertyComponent();
    expect(component).toBeTruthy();
  });
});
