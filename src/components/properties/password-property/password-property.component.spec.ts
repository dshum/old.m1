/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { PasswordPropertyComponent } from './password-property.component';

describe('Component: PasswordProperty', () => {
  it('should create an instance', () => {
    let component = new PasswordPropertyComponent();
    expect(component).toBeTruthy();
  });
});
